{
  "$schema_note": "Copy this file to ~/.claude/dvd-autorip-skill/config.local.json (NOT config/config.local.json here -- that old location is only ever read once, as a migration source) and fill in real values, or let the skill do it for you conversationally on first run. See references/config-schema.md for why the location is fixed outside the plugin tree, and for a full description of every field. Never commit a real config.local.json wherever it ends up.",

  "media_server": {
    "type": "jellyfin"
  },

  "jellyfin": {
    "base_url": "http://REPLACE_ME:8096",
    "api_key": "REPLACE_ME"
  },

  "library": {
    "movies_path": "REPLACE_ME e.g. D:\\Media\\movies",
    "shows_path": "REPLACE_ME e.g. D:\\Media\\shows",
    "naming": {
      "movie_template": "{title} ({year}).mkv",
      "show_template": "{show} - S{season:02d}E{episode:02d} - {episode_title}.mkv"
    }
  },

  "staging": {
    "path": "./_autorip_staging"
  },

  "bonus_content": {
    "handling": "ask",
    "remember_choice": false
  },

  "makemkv": {
    "path": null
  },

  "tesseract": {
    "path": null
  },

  "setup": {
    "declined_optional_installs": [],
    "model_effort_ack": false
  },

  "parallel": {
    "max_drives": null
  },

  "discdb": {
    "enabled": true
  },

  "library_duplicates": {
    "handling": "ask"
  }
}
